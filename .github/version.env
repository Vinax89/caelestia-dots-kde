# Canonical release metadata. Every runtime component that talks to GitHub
# derives its defaults from REPO; .github/scripts/check_repo_identity.py fails
# CI when a hardcoded default drifts away from this value.
VERSION=v2.3.2
REPO=Vinax89/caelestia-dots-kde
