[Unit]
Description=Caelestia Update Checker Service
# After= alone only orders the unit; without Wants= the target is never pulled
# in, so the check could run before the network was up.
Wants=network-online.target
After=network-online.target

[Service]
Type=oneshot
ExecStart=%h/.local/bin/caelestia-check-updates
