.SRCINFO
backups/
src/bin/build/

# Python
__pycache__/
*.py[cod]

installer/build/
caelestia-install

# Out-of-tree CMake build directories. Anchored to the ones that actually exist
# rather than a bare `build`, which matched any file or directory of that name
# at any depth in the tree.
shell/build/
shell/plugin/build/
kwin-effects/workspace-tracker/build/

kdotool/
run.sh
kde-material-you-colors/
kwin-effects-better-blur-dx/
plasma-workspace/